import React from 'react';

// Intentionally blank. Body background is handled in index.css.
// Kept as a no-op so existing imports continue to work.
export default function AppBackground() {
  return null;
}
